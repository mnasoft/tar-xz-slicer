#!/bin/bash

# === tar-xz-slicer: Stream-friendly архивация и распаковка ===
# 🛠 Упаковывает каталоги с xz-сжатием, нарезает на слайсы без tmp-файлов
# 📤 Распаковывает обратно, склеивая части на лету
# 📅 Версия: 0.1 | Автор: Микола + Copilot | Дата: 2025-06-30

# Цвета
GREEN="\033[0;32m"
YELLOW="\033[1;33m"
BLUE="\033[0;34m"
RED="\033[0;31m"
NC="\033[0m"

log_info()    { echo -e "${BLUE}[INFO]${NC} $1"; }
log_success() { echo -e "${GREEN}[OK]${NC} $1"; }
log_warn()    { echo -e "${YELLOW}[WARN]${NC} $1"; }
log_error()   { echo -e "${RED}[ERROR]${NC} $1"; }

print_usage() {
  echo -e "${YELLOW}USAGE:${NC}"
  echo -e "  tar-xz-slicer pack <каталог> [размер_слайса] [префикс]"
  echo -e "     Упаковывает каталог с xz-сжатием и разбивает по частям"
  echo -e "     Пример: tar-xz-slicer pack ./my_folder 512m backup_"
  echo
  echo -e "  tar-xz-slicer unpack <префикс>"
  echo -e "     Склеивает части и распаковывает архив"
  echo -e "     Пример: tar-xz-slicer unpack backup_"
  echo
  echo -e "  tar-xz-slicer --list"
  echo -e "     Показывает доступные команды"
  echo
}

pack_and_slice() {
  local full_path="$1"
  local slice_size="${2:-1024m}"
  local out_prefix="${3:-archive.part.}"

  if [[ ! -d "$full_path" ]]; then
    log_error "Каталог '$full_path' не найден"
    return 1
  fi

  local dirname="$(basename "$full_path")"
  local parentdir="$(dirname "$full_path")"
  local outdir="$(pwd)"  # каталог, откуда запускается скрипт

  log_info "Упаковка '${dirname}' с нарезкой по ${slice_size}, префикс: ${out_prefix}"

  (
    cd "$parentdir" || exit 1
    tar -c "$dirname" | xz -c | split -b "$slice_size" \
      --suffix-length=6 --numeric-suffixes=0 \
      - "${outdir}/${out_prefix}"
  )

  if [[ $? -eq 0 ]]; then
    log_success "Создано:"
    ls -1 "${outdir}/${out_prefix}"* | sort | while read -r f; do
      echo -e "  ${BLUE}$(basename "$f")${NC}"
    done
  else
    log_error "Ошибка при создании архива"
    return 1
  fi
}


unslice_and_unpack() {
  local slice_prefix="$1"

  if [[ -z "$slice_prefix" ]]; then
    log_error "Не указан префикс для распаковки"
    return 1
  fi

  if ! ls "${slice_prefix}"* &> /dev/null; then
    log_error "Слайсы с префиксом '${slice_prefix}' не найдены"
    return 1
  fi

  log_info "Объединяем слайсы и извлекаем архив..."
  cat "${slice_prefix}"* | xz -d | tar -x

  if [[ $? -eq 0 ]]; then
    log_success "Архив успешно восстановлен!"
  else
    log_error "Ошибка при распаковке"
    return 1
  fi
}

main() {
  case "$1" in
    pack)
      shift
      if [[ $# -lt 1 ]]; then
        log_warn "Не указан каталог для упаковки."
        print_usage
        return 1
      fi
      pack_and_slice "$@"
      ;;
    unpack)
      shift
      if [[ $# -lt 1 ]]; then
        log_warn "Не указан префикс для распаковки."
        print_usage
        return 1
      fi
      unslice_and_unpack "$@"
      ;;
    --help|-h)
      print_usage
      ;;
    --list)
      echo -e "🎛 Доступные команды:"
      echo "  pack     — Упаковать и нарезать"
      echo "  unpack   — Склеить и распаковать"
      echo "  --help   — Показать справку"
      echo "  --list   — Перечислить команды"
      ;;
    *)
      log_warn "Неизвестная команда: '$1'"
      print_usage
      return 1
      ;;
  esac
}

main "$@"
